# Developer/CI machine credentials for live smoke tests ONLY (never the product auth path).
# Copy to .env.local (gitignored) and fill in. Live tests run only when SIRV_LIVE=1.
SIRV_CLIENT_ID=
SIRV_CLIENT_SECRET=
SIRV_LIVE=0
